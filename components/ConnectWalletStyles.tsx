import styled from "styled-components"

import { DEVICE, COLORS } from "utils/variables"

export const ConnectWalletStyles = styled.div`
.wallet-icon{
    svg {
        width:${27.08/16}rem;
        height:${30.08/16}rem;;
        path {
          fill: #fff;
        }
  }
`